[package]
name = "mnemosyne-memory-core"
license = "MIT OR Apache-2.0"
version = "0.2.0"
edition = "2024"
rust-version = "1.95"
repository.workspace = true
description = "Core allocator types, policies, and validation for Mnemosyne"

[lib]
name = "mnemosyne_core"

[features]
default = ["std"]
std = []

[dependencies]
